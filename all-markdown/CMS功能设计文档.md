# 申允木业后台管理系统 - CMS功能完整设计文档

## 📋 目录

1. [项目概述](#项目概述)
2. [功能需求](#功能需求)
3. [UI界面设计](#ui界面设计)
4. [技术架构](#技术架构)
5. [数据结构设计](#数据结构设计)
6. [API接口设计](#api接口设计)
7. [实时预览系统](#实时预览系统)
8. [响应式系统](#响应式系统)
9. [实施计划](#实施计划)

---

## 项目概述

### 1.1 项目背景
为申允木业后台管理系统添加完整的CMS（内容管理系统）功能，使管理员能够：
- 实时编辑网站内容
- 实时预览修改效果
- 在不同设备尺寸下预览响应式效果
- 管理多媒体资源
- 版本控制和历史记录

### 1.2 设计目标
- **易用性**：直观的可视化编辑界面
- **实时性**：修改即时反映在预览中
- **响应式**：支持多设备预览
- **可扩展性**：易于添加新功能模块
- **稳定性**：数据安全，操作可追溯

---

## 功能需求

### 2.1 核心功能模块

#### 2.1.1 内容编辑器
- ✅ 富文本编辑器（支持格式化、链接、图片）
- ✅ 图片上传和管理
- ✅ 视频嵌入支持
- ✅ 代码块编辑（用于特殊内容）
- ✅ 字段验证和提示

#### 2.1.2 实时预览系统
- ✅ 实时同步编辑内容到预览窗口
- ✅ 多设备尺寸预览（手机、平板、桌面）
- ✅ 预览窗口独立控制（缩放、刷新）
- ✅ 预览与编辑同步滚动（可选）
- ✅ 预览状态指示器

#### 2.1.3 响应式预览
- ✅ 预设设备尺寸（iPhone、iPad、Desktop）
- ✅ 自定义尺寸设置
- ✅ 横屏/竖屏切换
- ✅ 设备像素比模拟
- ✅ 触摸事件模拟（移动端）

#### 2.1.4 页面管理
- ✅ 页面列表视图
- ✅ 页面状态管理（发布/草稿）
- ✅ 页面模板选择
- ✅ 页面SEO设置

#### 2.1.5 媒体库
- ✅ 图片库管理
- ✅ 图片分类和标签
- ✅ 图片搜索和筛选
- ✅ 图片编辑（裁剪、压缩）
- ✅ 批量上传

#### 2.1.6 版本控制
- ✅ 内容版本历史
- ✅ 版本对比
- ✅ 版本回滚
- ✅ 自动保存草稿

#### 2.1.7 发布管理
- ✅ 发布/取消发布
- ✅ 定时发布
- ✅ 发布前预览
- ✅ 发布日志

---

## UI界面设计

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  顶部导航栏 (TopBar)                                        │
│  [Logo] [页面选择] [保存] [预览] [发布] [用户菜单]          │
├──────────┬─────────────────────────────────────────────────┤
│          │                                                   │
│  左侧    │              主编辑区 (Main Editor)              │
│  侧边栏  │  ┌─────────────────────────────────────────┐     │
│  (Sidebar)│ │  内容编辑器 (Content Editor)            │     │
│          │ │  - 字段编辑表单                          │     │
│  - 页面  │ │  - 富文本编辑器                          │     │
│   列表   │ │  - 媒体选择器                            │     │
│  - 栏目  │ └─────────────────────────────────────────┘     │
│   导航   │                                                   │
│  - 媒体  │  ┌─────────────────────────────────────────┐     │
│   库     │ │  实时预览窗口 (Preview Window)            │     │
│          │ │  [设备选择] [刷新] [全屏]                 │     │
│          │ │  ┌───────────────────────────────────┐   │     │
│          │ │  │  预览内容 (实时同步)              │   │     │
│          │ │  └───────────────────────────────────┘   │     │
│          │ └─────────────────────────────────────────┘     │
│          │                                                   │
└──────────┴─────────────────────────────────────────────────┘
```

### 3.2 详细界面设计

#### 3.2.1 顶部导航栏 (TopBar)
```
┌─────────────────────────────────────────────────────────────┐
│  [SY Logo]  页面: [首页 ▼]  │  [💾 保存] [👁️ 预览] [🚀 发布] │
│                            │  [📊 统计] [⚙️ 设置] [👤 用户] │
└─────────────────────────────────────────────────────────────┘
```

**功能按钮说明：**
- **保存**：保存当前编辑内容（显示保存状态）
- **预览**：打开/关闭预览窗口
- **发布**：发布当前页面（带确认对话框）
- **统计**：查看页面访问数据
- **设置**：页面SEO、元数据设置
- **用户**：用户信息、退出登录

#### 3.2.2 左侧侧边栏 (Sidebar)

**页面列表区域：**
```
┌─────────────────────┐
│ 📄 页面管理          │
├─────────────────────┤
│ 🏠 首页              │
│ 📦 产品系列          │
│ 🖼️ 高定案例          │
│ 🛠️ 定制服务          │
│ ℹ️ 关于我们          │
│ 📞 联系我们          │
│ ⚙️ 全站设置          │
└─────────────────────┘
```

**栏目导航区域：**
```
┌─────────────────────┐
│ 📑 当前页面栏目      │
├─────────────────────┤
│ • 品牌信息           │
│ • 页脚信息           │
│ • 浮动侧边栏         │
│ • 首页头图           │
│ • 二维码区域         │
│ • 产品系列标签       │
│ • 产品画廊           │
│ • 精选案例           │
└─────────────────────┘
```

**媒体库区域：**
```
┌─────────────────────┐
│ 🖼️ 媒体库            │
├─────────────────────┤
│ [搜索框]             │
│ [分类筛选 ▼]        │
│                     │
│ [缩略图网格]         │
│ [图片1] [图片2]      │
│ [图片3] [图片4]      │
│                     │
│ [+ 上传图片]         │
└─────────────────────┘
```

#### 3.2.3 主编辑区 (Main Editor)

**内容编辑卡片：**
```
┌─────────────────────────────────────────────────┐
│ 栏目名称: 品牌信息                    [保存] [重置] │
├─────────────────────────────────────────────────┤
│                                                 │
│  品牌名称: [输入框]                              │
│  品牌标语: [输入框]                              │
│  品牌口号: [文本域]                              │
│                                                 │
│  品牌Logo: [图片预览] [上传图片] [选择媒体库]    │
│                                                 │
│  状态: ✓ 已保存                                  │
└─────────────────────────────────────────────────┘
```

**富文本编辑器：**
```
┌─────────────────────────────────────────────────┐
│ [B] [I] [U] [链接] [图片] [视频] [代码]           │
├─────────────────────────────────────────────────┤
│                                                 │
│  编辑区域（所见即所得）                          │
│                                                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

#### 3.2.4 实时预览窗口 (Preview Window)

**预览控制栏：**
```
┌─────────────────────────────────────────────────┐
│ 实时预览                          [🔄] [⛶] [✕] │
├─────────────────────────────────────────────────┤
│ 设备: [📱 iPhone 14 Pro ▼] [🔄 刷新] [📐 尺寸] │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────────────────┐               │
│  │                             │               │
│  │    网站预览内容              │               │
│  │    (实时同步显示)            │               │
│  │                             │               │
│  └─────────────────────────────┘               │
│                                                 │
└─────────────────────────────────────────────────┘
```

**设备选择菜单：**
```
┌─────────────────────┐
│ 📱 移动设备          │
│  • iPhone 14 Pro    │
│  • iPhone SE        │
│  • Samsung Galaxy   │
│                     │
│ 📱 平板设备          │
│  • iPad Pro         │
│  • iPad Air         │
│                     │
│ 💻 桌面设备          │
│  • Desktop (1920px) │
│  • Laptop (1366px)  │
│                     │
│ ⚙️ 自定义尺寸        │
└─────────────────────┘
```

### 3.3 响应式设计

#### 3.3.1 桌面端布局 (> 1024px)
- 左侧边栏：固定宽度 280px
- 主编辑区：自适应宽度
- 预览窗口：可调整宽度（最小 400px）
- 三栏布局：侧边栏 | 编辑区 | 预览区

#### 3.3.2 平板端布局 (768px - 1024px)
- 左侧边栏：可折叠抽屉
- 主编辑区：全宽显示
- 预览窗口：标签页切换或分屏显示
- 两栏布局：编辑区 | 预览区（可切换）

#### 3.3.3 移动端布局 (< 768px)
- 左侧边栏：底部导航或侧滑菜单
- 主编辑区：全屏显示
- 预览窗口：独立页面或模态框
- 单栏布局：编辑和预览通过标签切换

### 3.4 颜色方案

```css
/* 主色调 */
--primary: #4f46e5;        /* 主色：靛蓝色 */
--primary-dark: #4338ca;   /* 主色深色 */
--primary-light: #818cf8;  /* 主色浅色 */

/* 功能色 */
--success: #16a34a;        /* 成功：绿色 */
--warning: #f59e0b;        /* 警告：橙色 */
--error: #ef4444;          /* 错误：红色 */
--info: #3b82f6;           /* 信息：蓝色 */

/* 中性色 */
--bg: #f4f6fb;             /* 背景色 */
--card-bg: #ffffff;        /* 卡片背景 */
--text: #1f2933;           /* 主文本 */
--text-light: #6b7280;     /* 次要文本 */
--border: #e5e7eb;         /* 边框色 */

/* 预览窗口 */
--preview-bg: #f9fafb;     /* 预览背景 */
--preview-border: #d1d5db; /* 预览边框 */
```

### 3.5 交互设计

#### 3.5.1 编辑交互
- **实时保存提示**：输入后显示"有未保存的修改"
- **自动保存**：每30秒自动保存草稿
- **保存状态**：保存中/已保存/保存失败
- **字段验证**：实时验证，错误高亮显示

#### 3.5.2 预览交互
- **实时同步**：编辑后500ms内更新预览
- **滚动同步**：可选开启编辑区与预览区滚动同步
- **预览刷新**：手动刷新按钮
- **全屏预览**：点击全屏按钮进入全屏模式

#### 3.5.3 响应式预览交互
- **设备切换**：点击设备选择器快速切换
- **尺寸调整**：拖拽预览窗口边缘调整尺寸
- **旋转**：点击旋转按钮切换横竖屏
- **触摸模拟**：移动端预览支持触摸事件

---

## 技术架构

### 4.1 前端技术栈

#### 4.1.1 核心框架
- **基础**：原生 HTML5 + CSS3 + JavaScript (ES6+)
- **UI组件**：自定义组件库（基于现有设计）
- **状态管理**：自定义状态管理器
- **路由**：Hash路由（单页应用）

#### 4.1.2 编辑器组件
- **富文本编辑器**：Quill.js 或 TinyMCE
- **代码编辑器**：Monaco Editor（可选）
- **图片编辑器**：Cropper.js（裁剪功能）

#### 4.1.3 预览系统
- **iframe预览**：使用iframe加载预览页面
- **WebSocket**：实时通信（可选，用于多用户协作）
- **PostMessage**：iframe与父窗口通信

#### 4.1.4 工具库
- **HTTP请求**：Fetch API（封装）
- **数据验证**：自定义验证器
- **工具函数**：Lodash（可选）或自定义工具库

### 4.2 后端技术栈

#### 4.2.1 现有技术
- **框架**：Express.js
- **数据存储**：JSON文件（可扩展为数据库）
- **文件上传**：Multer
- **认证**：Token-based认证

#### 4.2.2 新增功能
- **WebSocket服务**：Socket.io（实时预览）
- **版本控制**：Git-like版本管理（JSON存储）
- **缓存系统**：内存缓存（预览内容）

### 4.3 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Frontend)                       │
├─────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 编辑器    │  │ 预览窗口  │  │ 媒体库    │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│       │              │              │                    │
│       └──────────────┼──────────────┘                    │
│                      │                                    │
│              ┌───────▼───────┐                           │
│              │  状态管理器    │                           │
│              └───────┬───────┘                           │
└──────────────────────┼───────────────────────────────────┘
                       │ HTTP/WebSocket
┌──────────────────────▼───────────────────────────────────┐
│                   后端 (Backend)                          │
├─────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 内容API  │  │ 预览API  │  │ 媒体API  │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│       │              │              │                    │
│       └──────────────┼──────────────┘                    │
│                      │                                    │
│              ┌───────▼───────┐                           │
│              │  数据层       │                           │
│              │  (JSON/DB)    │                           │
│              └───────────────┘                           │
└─────────────────────────────────────────────────────────┘
```

---

## 数据结构设计

### 5.1 内容数据结构

#### 5.1.1 页面内容结构（现有，扩展）
```json
{
  "global": {
    "brand": { ... },
    "footer": { ... }
  },
  "pages": {
    "home": { ... },
    "products": { ... }
  }
}
```

#### 5.1.2 版本控制结构（新增）
```json
{
  "versions": [
    {
      "id": "v1.0.0",
      "page": "home",
      "section": "hero",
      "content": { ... },
      "createdAt": "2024-01-01T00:00:00Z",
      "createdBy": "admin",
      "message": "初始版本"
    }
  ]
}
```

#### 5.1.3 草稿结构（新增）
```json
{
  "drafts": {
    "home": {
      "hero": {
        "content": { ... },
        "lastModified": "2024-01-01T00:00:00Z",
        "autoSaved": true
      }
    }
  }
}
```

#### 5.1.4 媒体库结构（新增）
```json
{
  "media": [
    {
      "id": "media_001",
      "filename": "image.jpg",
      "url": "/uploads/image.jpg",
      "type": "image",
      "size": 1024000,
      "width": 1920,
      "height": 1080,
      "tags": ["hero", "banner"],
      "category": "images",
      "uploadedAt": "2024-01-01T00:00:00Z",
      "uploadedBy": "admin"
    }
  ]
}
```

### 5.2 预览数据结构

#### 5.2.1 预览配置
```json
{
  "preview": {
    "device": "iphone-14-pro",
    "width": 393,
    "height": 852,
    "scale": 1,
    "orientation": "portrait",
    "userAgent": "Mozilla/5.0..."
  }
}
```

---

## API接口设计

### 6.1 内容管理API（扩展现有）

#### 6.1.1 获取内容
```
GET /api/admin/content
GET /api/admin/content/page/:page
GET /api/admin/content/page/:page/section/:section
```

#### 6.1.2 更新内容
```
PUT /api/admin/content/page/:page
PUT /api/admin/content/page/:page/section/:section
```

#### 6.1.3 重置内容
```
POST /api/admin/content/page/:page/reset
POST /api/admin/content/page/:page/section/:section/reset
```

### 6.2 预览API（新增）

#### 6.2.1 获取预览内容
```
GET /api/admin/preview/page/:page
GET /api/admin/preview/page/:page?device=iphone-14-pro
```

**响应示例：**
```json
{
  "success": true,
  "data": {
    "html": "<!DOCTYPE html>...",
    "css": "...",
    "js": "...",
    "content": { ... }
  }
}
```

#### 6.2.2 实时预览（WebSocket）
```
WS /api/admin/preview/ws
```

**消息格式：**
```json
// 客户端发送
{
  "type": "update",
  "page": "home",
  "section": "hero",
  "content": { ... }
}

// 服务端响应
{
  "type": "preview-updated",
  "html": "..."
}
```

### 6.3 版本控制API（新增）

#### 6.3.1 获取版本列表
```
GET /api/admin/versions/page/:page
GET /api/admin/versions/page/:page/section/:section
```

#### 6.3.2 创建版本
```
POST /api/admin/versions
Body: {
  "page": "home",
  "section": "hero",
  "content": { ... },
  "message": "更新首页头图"
}
```

#### 6.3.3 恢复版本
```
POST /api/admin/versions/:versionId/restore
```

#### 6.3.4 版本对比
```
GET /api/admin/versions/:versionId1/compare/:versionId2
```

### 6.4 草稿API（新增）

#### 6.4.1 保存草稿
```
POST /api/admin/drafts
Body: {
  "page": "home",
  "section": "hero",
  "content": { ... }
}
```

#### 6.4.2 获取草稿
```
GET /api/admin/drafts/page/:page
GET /api/admin/drafts/page/:page/section/:section
```

#### 6.4.3 删除草稿
```
DELETE /api/admin/drafts/page/:page/section/:section
```

### 6.5 媒体库API（新增）

#### 6.5.1 上传媒体
```
POST /api/admin/media/upload
Content-Type: multipart/form-data
Body: file, category, tags
```

#### 6.5.2 获取媒体列表
```
GET /api/admin/media
Query: ?category=images&tags=hero&page=1&limit=20
```

#### 6.5.3 删除媒体
```
DELETE /api/admin/media/:id
```

#### 6.5.4 更新媒体信息
```
PUT /api/admin/media/:id
Body: { "tags": [...], "category": "..." }
```

### 6.6 发布管理API（新增）

#### 6.6.1 发布页面
```
POST /api/admin/publish/page/:page
Body: {
  "publish": true,
  "scheduledAt": "2024-01-01T00:00:00Z" // 可选
}
```

#### 6.6.2 获取发布状态
```
GET /api/admin/publish/page/:page
```

#### 6.6.3 取消发布
```
POST /api/admin/publish/page/:page
Body: { "publish": false }
```

---

## 实时预览系统

### 7.1 预览架构

#### 7.1.1 预览流程
```
编辑内容 → 状态更新 → 防抖处理 → API请求 → 生成预览HTML → iframe更新
```

#### 7.1.2 预览实现方式

**方式一：iframe预览（推荐）**
```javascript
// 创建预览iframe
const previewIframe = document.createElement('iframe');
previewIframe.src = `/api/admin/preview/page/home?device=iphone-14-pro`;
previewContainer.appendChild(previewIframe);

// 更新预览内容
function updatePreview(content) {
  const message = {
    type: 'update-content',
    content: content
  };
  previewIframe.contentWindow.postMessage(message, '*');
}
```

**方式二：服务端渲染预览**
```javascript
// 后端生成完整HTML
app.get('/api/admin/preview/page/:page', async (req, res) => {
  const content = await getContentData(req.params.page);
  const html = await renderPreviewPage(content, req.query.device);
  res.send(html);
});
```

### 7.2 实时同步机制

#### 7.2.1 防抖处理
```javascript
let previewUpdateTimer;
function schedulePreviewUpdate(content) {
  clearTimeout(previewUpdateTimer);
  previewUpdateTimer = setTimeout(() => {
    updatePreview(content);
  }, 500); // 500ms防抖
}
```

#### 7.2.2 内容变更监听
```javascript
// 监听所有输入框变化
document.addEventListener('input', (e) => {
  if (e.target.matches('.content-editor input, .content-editor textarea')) {
    const content = collectFormData();
    schedulePreviewUpdate(content);
  }
});
```

### 7.3 预览窗口功能

#### 7.3.1 设备切换
```javascript
const DEVICES = {
  'iphone-14-pro': { width: 393, height: 852, userAgent: '...' },
  'ipad-pro': { width: 1024, height: 1366, userAgent: '...' },
  'desktop': { width: 1920, height: 1080, userAgent: '...' }
};

function switchDevice(deviceId) {
  const device = DEVICES[deviceId];
  previewIframe.style.width = `${device.width}px`;
  previewIframe.style.height = `${device.height}px`;
  updatePreviewUrl(deviceId);
}
```

#### 7.3.2 刷新预览
```javascript
function refreshPreview() {
  previewIframe.src = previewIframe.src; // 重新加载
}
```

#### 7.3.3 全屏预览
```javascript
function toggleFullscreen() {
  if (!document.fullscreenElement) {
    previewContainer.requestFullscreen();
  } else {
    document.exitFullscreen();
  }
}
```

---

## 响应式系统

### 8.1 设备预设

#### 8.1.1 移动设备
```javascript
const MOBILE_DEVICES = [
  {
    id: 'iphone-14-pro',
    name: 'iPhone 14 Pro',
    width: 393,
    height: 852,
    scale: 3,
    userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)'
  },
  {
    id: 'iphone-se',
    name: 'iPhone SE',
    width: 375,
    height: 667,
    scale: 2,
    userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 16_0 like Mac OS X)'
  },
  {
    id: 'samsung-galaxy-s21',
    name: 'Samsung Galaxy S21',
    width: 360,
    height: 800,
    scale: 3,
    userAgent: 'Mozilla/5.0 (Linux; Android 11; SM-G991B)'
  }
];
```

#### 8.1.2 平板设备
```javascript
const TABLET_DEVICES = [
  {
    id: 'ipad-pro',
    name: 'iPad Pro',
    width: 1024,
    height: 1366,
    scale: 2,
    userAgent: 'Mozilla/5.0 (iPad; CPU OS 16_0 like Mac OS X)'
  },
  {
    id: 'ipad-air',
    name: 'iPad Air',
    width: 820,
    height: 1180,
    scale: 2,
    userAgent: 'Mozilla/5.0 (iPad; CPU OS 16_0 like Mac OS X)'
  }
];
```

#### 8.1.3 桌面设备
```javascript
const DESKTOP_DEVICES = [
  {
    id: 'desktop-1920',
    name: 'Desktop (1920px)',
    width: 1920,
    height: 1080,
    scale: 1,
    userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'
  },
  {
    id: 'laptop-1366',
    name: 'Laptop (1366px)',
    width: 1366,
    height: 768,
    scale: 1,
    userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'
  }
];
```

### 8.2 自定义尺寸

```javascript
function createCustomDevice(width, height) {
  return {
    id: `custom-${width}x${height}`,
    name: `自定义 (${width}×${height})`,
    width: width,
    height: height,
    scale: 1,
    userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64)'
  };
}
```

### 8.3 响应式预览实现

#### 8.3.1 CSS媒体查询模拟
```javascript
function applyDeviceStyles(device) {
  const style = document.createElement('style');
  style.textContent = `
    @media (max-width: ${device.width}px) {
      /* 移动端样式 */
    }
  `;
  previewIframe.contentDocument.head.appendChild(style);
}
```

#### 8.3.2 视口设置
```html
<!-- 预览页面中 -->
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

### 8.4 触摸事件模拟

```javascript
// 在预览iframe中模拟触摸事件
function simulateTouchEvents() {
  const iframe = previewIframe.contentWindow;
  
  // 监听鼠标事件，转换为触摸事件
  iframe.document.addEventListener('mousedown', (e) => {
    const touchEvent = new TouchEvent('touchstart', {
      touches: [{
        clientX: e.clientX,
        clientY: e.clientY
      }]
    });
    e.target.dispatchEvent(touchEvent);
  });
}
```

---

## 实施计划

### 9.1 开发阶段

#### 阶段一：基础功能（2周）
- [ ] 扩展内容编辑器UI
- [ ] 实现基础预览窗口
- [ ] 实现内容保存功能
- [ ] 添加媒体上传功能

#### 阶段二：实时预览（2周）
- [ ] 实现iframe预览
- [ ] 实现实时同步机制
- [ ] 添加防抖处理
- [ ] 优化预览性能

#### 阶段三：响应式系统（1.5周）
- [ ] 实现设备预设
- [ ] 实现设备切换
- [ ] 实现自定义尺寸
- [ ] 添加触摸事件模拟

#### 阶段四：高级功能（2周）
- [ ] 实现版本控制
- [ ] 实现草稿保存
- [ ] 实现发布管理
- [ ] 添加媒体库管理

#### 阶段五：优化和测试（1.5周）
- [ ] 性能优化
- [ ] 兼容性测试
- [ ] 用户体验优化
- [ ] 文档完善

### 9.2 文件结构

```
shenyunmuye-admin-frontend/
├── admin.html (扩展)
├── cms/
│   ├── editor/
│   │   ├── editor.js (内容编辑器)
│   │   ├── rich-text.js (富文本编辑器)
│   │   └── field-renderer.js (字段渲染器)
│   ├── preview/
│   │   ├── preview.js (预览窗口)
│   │   ├── device-selector.js (设备选择器)
│   │   └── sync-manager.js (同步管理器)
│   ├── media/
│   │   ├── media-library.js (媒体库)
│   │   ├── uploader.js (上传组件)
│   │   └── image-editor.js (图片编辑器)
│   ├── version/
│   │   ├── version-control.js (版本控制)
│   │   └── version-comparison.js (版本对比)
│   └── utils/
│       ├── api.js (API封装)
│       ├── validator.js (验证器)
│       └── debounce.js (防抖工具)
└── styles/
    ├── cms.css (CMS样式)
    ├── editor.css (编辑器样式)
    └── preview.css (预览样式)
```

### 9.3 后端扩展

```
shenyunmuye-admin-backend/
├── server.js (扩展)
├── routes/
│   ├── content.js (内容路由)
│   ├── preview.js (预览路由，新增)
│   ├── version.js (版本路由，新增)
│   ├── draft.js (草稿路由，新增)
│   └── media.js (媒体路由，新增)
├── services/
│   ├── preview-service.js (预览服务，新增)
│   ├── version-service.js (版本服务，新增)
│   └── media-service.js (媒体服务，新增)
└── data/
    ├── versions/ (版本数据，新增)
    ├── drafts/ (草稿数据，新增)
    └── media/ (媒体数据，新增)
```

---

## 总结

本设计文档提供了完整的CMS功能设计方案，包括：

1. **完整的UI设计**：详细的界面布局和交互设计
2. **技术架构**：前后端技术选型和架构设计
3. **数据结构**：内容、版本、草稿、媒体等数据结构
4. **API设计**：完整的RESTful API接口
5. **实时预览**：iframe预览和实时同步机制
6. **响应式系统**：多设备预览和自定义尺寸
7. **实施计划**：分阶段开发计划

该方案基于现有项目架构，可以平滑扩展，不影响现有功能。所有新功能都采用模块化设计，便于维护和扩展。

---

**文档版本**：v1.0  
**创建日期**：2024年  
**最后更新**：2024年

